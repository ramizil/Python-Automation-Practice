# Exercise 09 — Hybrid UI + API (seed then verify)  🔜 (stub)

**Goal:** use the API to set up state fast, then verify behaviour through the UI.

## Outline
1. Create a record via the API (e.g. a restful-booker booking) in a fixture.
2. Open the UI and confirm the data is reflected (or act in UI, verify via API).
3. Tear down via the API after the test.

## Hints
- Extend the `booker` fixture, or add a worker/auto fixture that seeds + cleans up.
- API setup + UI assertion keeps tests fast and stable.

> Stub — implement it as practice (or ask me to flesh it out).
