"""Layer 2 — business steps, one package per product (saucedemo, booker, ...).

Each product package composes Layer-1 infrastructure and interaction primitives
(page objects / API clients) into reusable *business workflows*.
"""
